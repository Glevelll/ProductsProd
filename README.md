# Список продуктов по рецептам

Django веб-приложение для управления кулинарными рецептами и автоматического формирования списка покупок.

## Описание проекта

Веб-сервис позволяет:
- Регистрировать пользователей и управлять авторизацией
- Создавать, редактировать и удалять рецепты
- Импортировать рецепты с внешних сайтов (парсинг)
- Формировать список покупок на основе выбранных рецептов
- Автоматически суммировать ингредиенты из нескольких рецептов
- **REST API для программного доступа к данным** ✨
- **Аналитика данных с помощью pandas** ✨
- **ML-рекомендации рецептов через scikit-learn** ✨
- **Визуализация статистики с matplotlib и seaborn** ✨
- **Работа с БД через dbAPI (сырые SQL-запросы)** ✨

## Технологии

- **Backend:** Django 4.2.7
- **Frontend:** Django Templates, HTML, CSS
- **База данных:** PostgreSQL 15
- **ORM:** Django ORM
- **REST API:** Django REST Framework 3.14.0 ✨
- **dbAPI:** psycopg2 (прямые SQL-запросы) ✨
- **Анализ данных:** pandas 2.1.4, numpy 1.26.2 ✨
- **Машинное обучение:** scikit-learn 1.3.2 ✨
- **Визуализация:** matplotlib 3.8.2, seaborn 0.13.0 ✨
- **Парсинг:** requests, BeautifulSoup4
- **Тестирование:** Django TestCase
- **Контейнеризация:** Docker, docker-compose

## Структура проекта

```
ProductsProd/
├── config/                 # Конфигурация Django
│   ├── settings.py        # Настройки проекта
│   ├── urls.py            # Главные URL
│   ├── wsgi.py            # WSGI конфигурация
│   └── asgi.py            # ASGI конфигурация
├── recipes/               # Приложение рецептов
│   ├── models.py          # Модели данных
│   ├── views.py           # Представления
│   ├── forms.py           # Формы
│   ├── urls.py            # URL маршруты
│   ├── admin.py           # Админ-панель
│   ├── services.py        # Сервис парсинга
│   ├── serializers.py     # REST API сериализаторы ✨
│   ├── api_views.py       # REST API views ✨
│   ├── api_urls.py        # REST API маршруты ✨
│   ├── db_service.py     # dbAPI сервис ✨
│   ├── pandas_service.py # pandas анализ ✨
│   ├── ml_service.py     # ML рекомендации ✨
│   ├── visualization_service.py # Визуализация ✨
│   ├── analytics_views.py # Views для аналитики ✨
│   ├── tests.py          # Тесты
│   └── test_new_features.py # Тесты новых функций ✨
├── users/                 # Приложение пользователей
│   ├── views.py           # Представления
│   ├── forms.py           # Формы
│   └── urls.py            # URL маршруты
├── templates/             # HTML шаблоны
│   ├── base.html          # Базовый шаблон
│   ├── recipes/           # Шаблоны рецептов
│   └── users/             # Шаблоны пользователей
├── static/                # Статические файлы
│   └── css/               # Стили
├── Dockerfile             # Docker конфигурация
├── docker-compose.yml     # Docker Compose
├── requirements.txt       # Python зависимости
├── .env.example           # Пример переменных окружения
└── README.md              # Документация

```

## Быстрый старт

### Предварительные требования

- Docker
- Docker Compose

### Установка и запуск

1. **Клонируйте репозиторий (или перейдите в директорию проекта):**

```bash
cd ProductsProd
```

2. **Создайте файл .env из примера (если его нет):**

```bash
cp .env.example .env
```

Файл `.env` уже настроен со следующими параметрами:
```env
DEBUG=True
SECRET_KEY=django-insecure-dev-key-12345-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

DB_NAME=recipes_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
```

3. **Запустите проект с помощью Docker Compose:**

```bash
docker-compose up --build
```

При первом запуске Docker:
- Скачает необходимые образы
- Создаст контейнеры для Django и PostgreSQL
- Установит все зависимости
- Выполнит миграции базы данных
- Соберет статические файлы

4. **Откройте браузер и перейдите по адресу:**

```
http://localhost:8000
```

5. **Создайте суперпользователя для доступа к админ-панели:**

В новом терминале выполните:

```bash
docker-compose exec web python manage.py createsuperuser
```

Следуйте инструкциям для создания администратора.

6. **Доступ к админ-панели:**

```
http://localhost:8000/admin/
```

## Использование приложения

### Регистрация и вход

1. Перейдите на страницу регистрации: http://localhost:8000/users/register/
2. Заполните форму регистрации
3. После регистрации вы будете автоматически авторизованы

### Создание рецепта

1. Нажмите "Добавить рецепт" в меню
2. Заполните форму с информацией о рецепте:
   - Название
   - Описание
   - Инструкции
   - Время приготовления
   - Количество порций
   - URL источника (опционально)
3. Сохраните рецепт
4. Добавьте ингредиенты к рецепту

### Импорт рецепта с внешнего сайта

1. Нажмите "Импорт рецепта" в меню
2. Вставьте URL рецепта с внешнего сайта
3. Система автоматически извлечет:
   - Название рецепта
   - Описание
   - Список ингредиентов
   - Инструкции по приготовлению
   - Время приготовления и количество порций

### Формирование списка покупок

1. Просмотрите рецепты на главной странице
2. Откройте понравившийся рецепт
3. Нажмите "Добавить в список покупок"
4. Перейдите в "Список покупок" в меню
5. Система автоматически суммирует все ингредиенты из выбранных рецептов

## Новые возможности ✨

### REST API (webAPI)

Полноценный REST API для программного доступа к данным:

```bash
# Список рецептов
GET http://localhost:8000/api/recipes/

# Детали рецепта
GET http://localhost:8000/api/recipes/1/

# Создать рецепт
POST http://localhost:8000/api/recipes/

# Статистика
GET http://localhost:8000/api/recipes/stats/

# Список покупок
GET http://localhost:8000/api/shopping-list/
```

### dbAPI (прямые SQL-запросы)

Работа с PostgreSQL через psycopg2:
- Прямые SQL-запросы для сложной аналитики
- Оптимизированные JOIN-запросы
- Статистика через GROUP BY и агрегатные функции

Доступно на: `/statistics/dbapi/`

### pandas (анализ данных)

Анализ данных через pandas:
- Статистика рецептов (среднее, медиана, корреляция)
- Анализ ингредиентов
- Экспорт данных в Excel/CSV
- Временные ряды и тренды

Функции:
- 📊 Детальная статистика: `/statistics/`
- 📥 Экспорт в Excel: `/export/excel/`
- 📥 Экспорт в CSV: `/export/csv/`

### scikit-learn (машинное обучение)

ML-рекомендации и анализ:
- **TF-IDF + косинусное сходство** для поиска похожих рецептов
- **K-Means кластеризация** для группировки рецептов
- **Предсказание сложности** рецепта
- **Анализ важности ингредиентов**
- **Рекомендации по ингредиентам**

Доступно на: `/ml/recommendations/`

API endpoints:
- `GET /ml-api/similar/{recipe_id}/` - похожие рецепты
- `POST /ml-api/by-ingredients/` - рекомендации по ингредиентам
- `GET /ml-api/clusters/` - кластеризация

### matplotlib & seaborn (визуализация)

Графики и диаграммы:
- 📈 Распределение времени приготовления
- 📊 Распределение по порциям
- 🏆 Топ ингредиентов
- 👥 Рецепты по авторам
- 📅 Динамика создания рецептов
- 🔬 Корреляционные матрицы
- 📉 Scatter plots

Доступно на: `/analytics/`

## Запуск тестов

Для запуска unit-тестов выполните:

```bash
docker-compose exec web python manage.py test
```

Тесты покрывают:
- Регистрацию и авторизацию пользователей
- CRUD операции для рецептов
- Парсинг рецептов
- Формирование списка покупок
- REST API
- dbAPI
- pandas
- ML рекомендации
- Визуализацию

## Остановка проекта

Для остановки контейнеров:

```bash
docker-compose down
```

Для остановки и удаления всех данных (включая БД):

```bash
docker-compose down -v
```

## Разработка без Docker

Если вы хотите запустить проект локально без Docker:

1. **Установите Python 3.11+ и PostgreSQL**

2. **Создайте виртуальное окружение:**

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

4. **Настройте PostgreSQL:**

Создайте базу данных:
```sql
CREATE DATABASE recipes_db;
CREATE USER postgres WITH PASSWORD 'postgres';
GRANT ALL PRIVILEGES ON DATABASE recipes_db TO postgres;
```

5. **Обновите .env файл:**

Измените `DB_HOST=db` на `DB_HOST=localhost`

6. **Выполните миграции:**

```bash
python manage.py migrate
```

7. **Создайте суперпользователя:**

```bash
python manage.py createsuperuser
```

8. **Соберите статические файлы:**

```bash
python manage.py collectstatic
```

9. **Запустите сервер:**

```bash
python manage.py runserver
```

## Модели данных

### Recipe (Рецепт)
- title - название рецепта
- description - краткое описание
- instructions - инструкции по приготовлению
- author - автор рецепта (связь с User)
- cooking_time - время приготовления
- servings - количество порций
- source_url - источник рецепта

### Ingredient (Ингредиент)
- name - название ингредиента
- unit - единица измерения

### RecipeIngredient (Ингредиент в рецепте)
- recipe - рецепт
- ingredient - ингредиент
- quantity - количество

### ShoppingList (Список покупок)
- user - пользователь
- recipe - рецепт
- added_at - дата добавления

## Использованные технологии и библиотеки

### Backend & Framework
- ✅ **Django 4.2.7** - веб-фреймворк
- ✅ **Django REST Framework** - REST API
- ✅ **django-cors-headers** - CORS поддержка

### База данных
- ✅ **PostgreSQL 15** - СУБД
- ✅ **psycopg2-binary** - драйвер PostgreSQL (dbAPI)

### Анализ данных
- ✅ **pandas** - анализ и обработка данных
- ✅ **numpy** - численные вычисления
- ✅ **openpyxl** - работа с Excel

### Машинное обучение
- ✅ **scikit-learn** - ML алгоритмы
  - TF-IDF векторизация
  - Косинусное сходство
  - K-Means кластеризация
  - PCA (декомпозиция)

### Визуализация
- ✅ **matplotlib** - создание графиков
- ✅ **seaborn** - статистическая визуализация

### Парсинг
- ✅ **requests** - HTTP-запросы
- ✅ **beautifulsoup4** - парсинг HTML
- ✅ **lxml** - парсер

### Разное
- ✅ **python-dotenv** - переменные окружения
- ✅ **Pillow** - работа с изображениями

## Примеры использования

### 1. Работа с REST API

```python
import requests

# Получить список рецептов
response = requests.get('http://localhost:8000/api/recipes/')
recipes = response.json()

# Получить статистику
response = requests.get('http://localhost:8000/api/recipes/stats/')
stats = response.json()
```

### 2. ML-рекомендации

```python
# Получить похожие рецепты
response = requests.get('http://localhost:8000/ml-api/similar/1/')
similar = response.json()

# Рекомендации по ингредиентам
response = requests.post(
    'http://localhost:8000/ml-api/by-ingredients/',
    json={'ingredient_ids': [1, 2, 3]}
)
recommendations = response.json()
```

### 3. Экспорт данных

```python
# Скачать Excel
import pandas as pd

response = requests.get('http://localhost:8000/export/excel/')
with open('recipes.xlsx', 'wb') as f:
    f.write(response.content)

df = pd.read_excel('recipes.xlsx')
```

## API Документация

### REST API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/` | API root |
| GET | `/api/recipes/` | Список рецептов |
| POST | `/api/recipes/` | Создать рецепт |
| GET | `/api/recipes/{id}/` | Детали рецепта |
| PUT | `/api/recipes/{id}/` | Обновить рецепт |
| DELETE | `/api/recipes/{id}/` | Удалить рецепт |
| GET | `/api/recipes/stats/` | Статистика |
| GET | `/api/ingredients/` | Список ингредиентов |
| GET | `/api/shopping-list/` | Список покупок |
| GET | `/api/shopping-list/aggregated/` | Суммированный список |

### ML API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/ml-api/similar/{id}/` | Похожие рецепты (ML) |
| POST | `/ml-api/by-ingredients/` | Рекомендации по ингредиентам |
| GET | `/ml-api/clusters/` | Кластеризация K-Means |
| GET | `/dbapi/search/` | Поиск через SQL |

### Web Endpoints (UI)

| URL | Описание |
|-----|----------|
| `/` | Главная страница |
| `/analytics/` | Аналитика (matplotlib/seaborn) |
| `/statistics/` | Статистика (pandas) |
| `/statistics/dbapi/` | Статистика (dbAPI/SQL) |
| `/ml/recommendations/` | ML-рекомендации |
| `/export/excel/` | Экспорт в Excel |
| `/export/csv/` | Экспорт в CSV |

## Возможные проблемы и решения

### Ошибка подключения к базе данных

Убедитесь, что контейнер PostgreSQL запущен:
```bash
docker-compose ps
```

### Порт 8000 уже занят

Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"
```

### Ошибки миграций

Пересоздайте миграции:
```bash
docker-compose exec web python manage.py migrate --run-syncdb
```

## Автор

Итоговый проект по курсу «Язык Python и его библиотеки»

Демонстрирует использование:
- webAPI (Django REST Framework)
- dbAPI (psycopg2)
- pandas (анализ данных)
- scikit-learn (машинное обучение)
- matplotlib & seaborn (визуализация)

## Лицензия

Учебный проект

